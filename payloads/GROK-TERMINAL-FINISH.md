# GROK TERMINAL FINISH — Command Centre Hub tab

Paste this file into Grok in the terminal. Build and finish. Do not invent.

## Identity

- Org source: `szl-holdings` on GitHub (canonical)
- Artifact registry: Hugging Face `SZLHOLDINGS`
- Command Center: repo `szl-holdings/a11oy` · Space `SZLHOLDINGS/a11oy` · product `https://a-11-oy.com`
- Proof: `https://a11oy.net`
- Never: `a11oy.com`
- Bind package: `szl-holdings/lyte-lattice` · Space twin `SZLHOLDINGS/lyte-lattice` if present
- Plane that sealed N1–N27: Alloy State Fabric
- Doctrine v11 LOCKED · Λ = Conjecture 1 · signer HASH-LINKED / UNSIGNED-honest
- Hub write only from the protected publisher with a bound token and exact post-deploy readback
- Never fabricate joules, DSSE, Cosign, GPU trains, or RUNNING without Hub readback

## Done already (do not redo as mocks)

Waves acted on origin lyte organs:

| Wave | Cells | Result |
| --- | --- | --- |
| W1 Core path | N1–N9 | 9 admitted |
| W2 Observe + shape | N10–N18 | 9 admitted (N17 mesh hologram tag `vllm`) |
| W3 Plane edge | N19–N27 | 8 admitted · N27 GPU train BLOCKED · N26 wrap joule REPORTED null |

Packets on `szl-holdings/lyte-lattice`:

- `packets/alloy-state-fabric-w1.json`
- `packets/alloy-state-fabric-w2.json`
- `packets/alloy-state-fabric-w3.json`
- `packets/alloy-state-fabric-frontier.json`
- `packets/alloy-state-fabric-waves.json`

## Job 1 — Command Center tab

In `szl-holdings/a11oy`:

1. Keep `/lyte` as a real page-link tab (already in `a11oy_nav_wireup.py` `_SURFACES`).
2. Make `/lyte` render the N1–N27 wave board (W1/W2/W3) with honesty labels LIVE / MEASURED / REPORTED / UNAVAILABLE.
3. Embed or iframe is allowed only if the lyte-lattice Space is RUNNING after Hub readback. Otherwise serve a local hologram page that cites origin packets. Do not fake RUNNING.
4. Add the tab to the related-surfaces strip.
5. Product path `https://a-11-oy.com/lyte` must stay the public tab. Space twin follows after exact-source relock.

Do not turn lyte-lattice into a second flagship.

## Job 2 — GitHub org lanes (finish order)

Code these ten first. 122 repos exist; do not flatten the estate.

1. `a11oy` — wire the tab, keep receipts.in ≡ receipts.out
2. `lyte-lattice` — keep organs honest; do not invent energy_uj
3. `holographic-unify` — hologram only, not a-11-oy.com
4. `szl-hf-frontier` — read-only auditor, fail closed
5. `immune` — SENTRA/YAWAR
6. `hatun-mcp` — 16 tools
7. `szl-forge` — own-hardware kit; NEVER_DISPATCH gpu-bridge from console
8. `vertical-services` — five engines, honest labels
9. `szl-khipu` — kernels + tiny silhouettes
10. `.github` — org profile + doctrine workflows

For each lane: README honesty table, Apache-2.0, doctrine v11 badge, product ≠ proof, Hub is mirror.

## Job 3 — Publish on Hugging Face SZLHOLDINGS

Only with `HF_TOKEN` in the terminal environment.

```bash
python3 scripts/command_centre_payload.py --check
# then, if token present and origin tip is exact:
python3 scripts/command_centre_payload.py --publish-space
```

`--publish-space` must:

- Push `szl-holdings/lyte-lattice` Dockerfile-derived Space to `SZLHOLDINGS/lyte-lattice`
- Set README YAML `short_description` to the honesty line already on origin
- Wait for Hub readback of exact commit
- Only then print `RUNNING`
- If token missing: print `UNAVAILABLE` and stop. Do not mock the Space.

Also verify listings:

- `https://huggingface.co/SZLHOLDINGS`
- `https://huggingface.co/spaces/SZLHOLDINGS/a11oy`
- `https://huggingface.co/SZLHOLDINGS/SZL-Khipu-1.5B`

## Honesty lock

- K3 / Kimi is observed, not ours
- Mooncake / LMCache / NIXL UNAVAILABLE
- Production DSSE / Cosign not claimed from HASH-LINKED plane
- GPU QLoRA / Unsloth not dispatched from Alloy State Fabric
- Energy joule only if RAPL `energy_uj` reads
- Λ stays Conjecture 1

## Accept

- `/lyte` tab visible on Command Center and a-11-oy.com/lyte
- W1 W2 W3 board shows origin hashes
- Space `SZLHOLDINGS/lyte-lattice` RUNNING only after readback
- Ten lanes have current honesty READMEs
- No fabricated train, joule, or certificate
